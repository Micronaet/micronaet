<%
' ************** LIBRERIE PER LE FUNZIONI DI VALIDAZIONE CAMPI **********************

function isData(Valore)
    if isdate(valore) then
       isdata=true
    else
       isdata=false
    end if
end function

function isNumero(Valore, LoRange, HiRange)
    
end function

%>
